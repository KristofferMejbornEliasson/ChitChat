edition = "2023";

option go_package = "./chitchat";

service ChitChat {
  rpc RouteChat(stream Message) returns (stream Message) {}
}

message Message {
  string text = 1;
  int32 id = 2;
  repeated int64 clock = 3;
}